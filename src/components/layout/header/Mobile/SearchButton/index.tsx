import { Button, Loupe } from "components/layout/header/Mobile/SearchButton/styles";
import { FunctionComponent } from "react";

export const SearchButton: FunctionComponent = () => {
	return <Button>
		<Loupe/>
	</Button>;
};